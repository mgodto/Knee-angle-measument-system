# 下肢全長X線 自動計測 — 医師向けクイックガイド

## 動作環境

- 現在の検証済み版：Apple Silicon（M1以降）、macOS 12.1以降
- Python、PyTorch、OpenCVなどの追加インストール：不要
- GPU：不要（すべてCPUで実行）
- インターネット接続：不要。画像や計測結果を外部へ送信しません

配布時は `KneeXrayMeasurement-macOS-arm64.zip` を展開し、
`KneeXrayMeasurement.app` を起動してください。Windows版はWindows x64上で
別途ビルド・検証した配布パッケージが必要です。

## 基本操作

1. 「X線画像を開く…」から、片側下肢全長X線画像を選択します。
2. ファイル名から左右を判定できない場合は、上部で `L` または `R` を選択します。
3. AI解析後、右側に次の結果が表示されます。
   - 8個の解剖学的ランドマーク
   - 2本の関節線（4端点）
   - mLDFA、MPTA、JLCA、HKA
4. 必要に応じて左側画像のマーカーをドラッグします。離すと角度を再計算します。
5. 「結果を書き出す…」から、計測結果画像（PNG）と追跡情報（JSON）を保存します。

JSONには、座標、角度、左右、手動修正箇所、AIモデルのバージョンとSHA256が
記録されます。

## AIモデルの更新

「AIモデル…」→「AIモデルファイルを選択…」から、互換性のある `.pt` / `.pth` を
選択できます。アプリはモデル構造、前処理、ランドマーク順序、有限値出力を
検証してから適用します。検証済みファイルはアプリ専用データ領域へコピーされるため、
選択元のUSBメモリやダウンロードファイルを取り外しても使用できます。

同じ `small_heatmap_v1` インターフェースで再学習したスキーマバージョン1のモデルであれば、
GUIを変更せずモデルファイルだけを更新できます。「標準モデルに戻す」で内蔵モデルへ
戻せます。外部モデルが破損・消失した場合も、次回起動時に標準モデルへ戻ります。

## 対応範囲と注意

- 対応画像：JPG、PNG、BMP、TIFF
- 未対応：DICOM、両側を含む未切り出し画像
- 現在の内蔵モデルは研究用です。すべてのランドマークと角度を医師が確認してください。
- 現在の検証値はランドマークMAE 46.7 px、mLDFA MAE 25.05°、MPTA MAE 14.44°で、
  臨床使用に十分な精度ではありません。
- 本ソフトウェアを診断または治療方針の唯一の根拠として使用しないでください。

正式な院内配布前には、独立した臨床検証、組織のApple Developer IDによる署名、
notarization、および対象端末での受入試験が必要です。
